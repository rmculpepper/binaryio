#lang info

;; ========================================
;; pkg info

(define version "1.1")
(define collection "binaryio")
(define deps '("base"))

;; ========================================
;; collect info

(define name "binaryio")
